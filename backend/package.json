{
  "name": "groundtruth-backend",
  "version": "0.1.0",
  "private": true,
  "description": "API Gateway + Lambda backend for GroundTruth",
  "scripts": {
    "test": "echo \"no tests yet\" && exit 0",
    "lint": "echo \"no linter configured yet\" && exit 0"
  }
}
